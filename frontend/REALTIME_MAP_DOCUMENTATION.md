# 🗺️ Real-Time Map System for Hospital Portal

## ภาพรวม

ระบบแผนที่แบบเรียลไทม์สำหรับ Hospital Portal ที่แสดงตำแหน่งเคสฉุกเฉินบนแผนที่ พร้อมฟีเจอร์ครบครัน:

### ✨ ฟีเจอร์หลัก

1. **แผนที่แบบเรียลไทม์**
   - อัพเดทข้อมูลทุก 15 วินาที
   - แสดงตำแหน่งเคสตามพิกัด GPS จริง
   - รองรับการซูมและเลื่อนแผนที่

2. **หมุดแยกสีตามความรุนแรง**
   - 🔴 Grade 4 (วิกฤติ) - สีแดงเข้ม (#dc2626)
   - 🟠 Grade 3 (เร่งด่วน) - สีส้ม (#f97316)
   - 🟡 Grade 2 (ปานกลาง) - สีเหลือง (#eab308)
   - 🟢 Grade 1 (ไม่เร่งด่วน) - สีเขียว (#22c55e)
   - ⚪ Unknown - สีเทา (#6b7280)

3. **Popup รายละเอียดเคส**
   - ข้อมูลผู้ป่วย (ชื่อ, เบอร์โทร)
   - ที่อยู่และตำแหน่ง
   - ประเภทฉุกเฉินและอาการ
   - เวลาที่รายงาน
   - ปุ่มนำทางไปยัง Google Maps
   - ปุ่มมอบหมายทีมกู้ภัย

4. **Legend และ Stats**
   - แสดงความหมายของสีหมุด
   - จำนวนเคสทั้งหมด
   - จำนวนเคสเร่งด่วน
   - สถานะ Live indicator

## 📁 ไฟล์ที่สร้างขึ้น

### 1. RealTimeMap Component
**ไฟล์:** `app/hospital/cases/components/RealTimeMap.tsx`

Component หลักสำหรับแสดงแผนที่แบบเรียลไทม์

**Props:**
```typescript
interface RealTimeMapProps {
  cases: EmergencyCase[];           // รายการเคสที่จะแสดงบนแผนที่
  selectedCaseId?: string | null;   // ID ของเคสที่เลือก
  onCaseSelect?: (caseId: string) => void;  // Callback เมื่อคลิกเลือกเคส
  onTransferCase?: (caseId: string) => void; // Callback เมื่อต้องการมอบหมายทีม
  className?: string;                // CSS class
  autoRefresh?: boolean;             // เปิด/ปิด auto refresh (default: true)
  refreshInterval?: number;          // ระยะเวลา refresh (ms, default: 15000)
}
```

**ฟีเจอร์:**
- Custom marker icons แยกสีตามความรุนแรง
- Popup แสดงรายละเอียดเคส
- Auto-center เมื่อเลือกเคส
- Animation สำหรับเคสเร่งด่วน (pulse effect)
- Legend แสดงความหมายสี
- Stats overlay

### 2. Full-Screen Map Page
**ไฟล์:** `app/hospital/cases/map/page.tsx`

หน้าแผนที่แบบเต็มหน้าจอพร้อม sidebar สำหรับกรองและเลือกเคส

**ฟีเจอร์:**
- Sidebar แสดงรายการเคสทั้งหมด
- ตัวกรองตามสถานะและความรุนแรง
- ค้นหาเคส
- Detail panel สำหรับเคสที่เลือก
- ปุ่มซ่อน/แสดง sidebar
- Auto refresh ทุก 15 วินาที

### 3. Updated Cases Page
**ไฟล์:** `app/hospital/cases/page.tsx`

อัพเดทหน้า Emergency Cases ให้ใช้ RealTimeMap แทน MapView เดิม

**การเปลี่ยนแปลง:**
- เปลี่ยนจาก MapView เป็น RealTimeMap
- เพิ่ม auto refresh
- ปรับปรุง UI สำหรับ map view

### 4. Updated Dashboard Layout
**ไฟล์:** `components/dashboard/dashboard-layout.tsx`

เพิ่มเมนู "Real-Time Map" สำหรับ Hospital Portal

**การเปลี่ยนแปลง:**
- เพิ่ม MapPin icon import
- เพิ่มเมนู "Real-Time Map" ที่ลิงก์ไปยัง `/hospital/cases/map`
- จัดเรียงเมนูใหม่ให้เป็นระเบียบ

## 🚀 การใช้งาน

### 1. ดูแผนที่แบบฝังในหน้า Cases
```
/hospital/cases
```
- สลับระหว่าง List View และ Map View ได้
- แผนที่จะแสดงในส่วนล่างของหน้า

### 2. ดูแผนที่แบบเต็มหน้าจอ
```
/hospital/cases/map
```
- เข้าผ่านเมนู "Real-Time Map" ใน sidebar
- มี sidebar สำหรับกรองและเลือกเคส
- แสดง detail panel เมื่อเลือกเคส

## 🎨 การออกแบบ

### สีหมุดตามความรุนแรง
```typescript
const getSeverityColor = (severity: number): string => {
  switch (severity) {
    case 4: return "#dc2626"; // Critical - แดงเข้ม
    case 3: return "#f97316"; // Urgent - ส้ม
    case 2: return "#eab308"; // Moderate - เหลือง
    case 1: return "#22c55e"; // Non-urgent - เขียว
    default: return "#6b7280"; // Unknown - เทา
  }
};
```

### Animation
- **Pulse effect** สำหรับเคส Grade 3-4 (เร่งด่วน)
- **Smooth transitions** เมื่อเลือกเคสหรือซูมแผนที่
- **Hover effects** บน markers และ UI elements

## 🔧 การตั้งค่า

### Auto Refresh
```typescript
// ใน RealTimeMap component
<RealTimeMap
  cases={filteredCases}
  autoRefresh={true}           // เปิด auto refresh
  refreshInterval={15000}      // refresh ทุก 15 วินาที
/>
```

### Custom Styling
```typescript
// เพิ่ม className ได้
<RealTimeMap
  cases={filteredCases}
  className="h-full w-full rounded-lg"
/>
```

## 📊 Data Flow

```
1. useHospitalCases Hook
   ↓
2. Fetch cases from API (/hospital/assigned-cases)
   ↓
3. Filter & normalize data
   ↓
4. Pass to RealTimeMap component
   ↓
5. Render markers on map
   ↓
6. Auto refresh every 15s
```

## 🎯 ฟีเจอร์ที่สามารถเพิ่มเติมได้

1. **WebSocket Integration**
   - Real-time updates โดยไม่ต้อง polling
   - แจ้งเตือนทันทีเมื่อมีเคสใหม่

2. **Clustering**
   - รวมหมุดที่อยู่ใกล้กันเมื่อซูมออก
   - ใช้ `react-leaflet-cluster`

3. **Heatmap Layer**
   - แสดงความหนาแน่นของเคสในแต่ละพื้นที่
   - ใช้ `leaflet.heat`

4. **Route Planning**
   - แสดงเส้นทางจากโรงพยาบาลไปยังเคส
   - ใช้ Routing API

5. **Geofencing**
   - กำหนดพื้นที่ coverage ของโรงพยาบาล
   - แจ้งเตือนเมื่อเคสอยู่นอกพื้นที่

## 🐛 Troubleshooting

### แผนที่ไม่แสดง
1. ตรวจสอบว่า `leaflet` และ `react-leaflet` ติดตั้งแล้ว
2. ตรวจสอบว่า CSS ของ Leaflet ถูก import
3. ตรวจสอบว่าใช้ `dynamic import` กับ `ssr: false`

### หมุดไม่แสดง
1. ตรวจสอบว่า cases มี `location.coordinates.lat` และ `lng`
2. ตรวจสอบว่าพิกัดอยู่ในช่วงที่ถูกต้อง (-90 to 90 lat, -180 to 180 lng)

### Performance Issues
1. ลด `refreshInterval` ถ้าไม่จำเป็น
2. ใช้ clustering สำหรับเคสจำนวนมาก
3. Optimize การ render ด้วย `React.memo`

## 📝 Notes

- แผนที่ใช้ OpenStreetMap tiles (ฟรี, ไม่ต้อง API key)
- รองรับ dark mode
- Responsive design สำหรับ mobile
- Accessibility features (keyboard navigation, ARIA labels)

## 🔗 Related Files

- `app/hospital/cases/hooks/useHospitalCases.ts` - Hook สำหรับดึงข้อมูลเคส
- `app/shared/types.ts` - Type definitions
- `app/shared/services/emergencyService.ts` - API service layer

---

**Created:** 2025-11-26
**Version:** 1.0.0
**Status:** ✅ Ready for Production
